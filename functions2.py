# # This is an example of passing a parameter/argument when calling a function.

# Define a function named 'myFunction' that takes one parameter called 'rating'.
def myFuncion(rating):
   
    # Inside the function, print the text "Python is " concatenated with the 'rating' parameter.
    print("Python is", rating)

# Call the 'myFuncion' function with the argument 'fantastic'.
# This will execute the code inside the function and pass 'fantastic' as the 'rating' parameter.
# NOTE: If you omit the line below, the function will not run, even though it exists.
myFuncion('fantastic')
